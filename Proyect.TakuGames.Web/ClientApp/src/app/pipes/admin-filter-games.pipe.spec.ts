import { AdminFilterGamesPipe } from './admin-filter-games.pipe';

describe('AdminFilterGamesPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminFilterGamesPipe();
    expect(pipe).toBeTruthy();
  });
});
